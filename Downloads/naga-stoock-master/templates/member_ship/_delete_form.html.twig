<form method="post" action="{{ path('member_ship_delete', {'id': member_ship.id}) }}" onsubmit="return confirm('L'adhésion va être supprimée pour de bon. Êtes-vous sûr ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ member_ship.id) }}">
    <button data-infoBulle="{{'btn.tooltip.delete'| trans}}"> <i class="far fa-trash-alt"></i> </button>
</form>

